/// ConfigKey enum is used to define the key name that we will store.
enum ConfigKey {
  googleClientId,
  googleClientSecret,
  oneDriveClientId,
  oneDriveClientSecret,
  oneDriveTenantId,
  s3AccessKey,
  s3SecretKey,
  s3Region,
  s3BucketId,
}